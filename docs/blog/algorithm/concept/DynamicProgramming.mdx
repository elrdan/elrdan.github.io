---
sidebar_position: 1
sidebar_label: 동적 프로그래밍(DPTopDown)
keywords: [알고리즘, java, 다이나믹 프로그래밍, DPTopDown, 동적 계획법]
hide_title: true
---

import CodeBlock from '@theme/CodeBlock';
import BottomUp from '@code/generated/DPBottomUp.json'
import TopDown from '@code/generated/DPTopDown.json'

## 다이나믹 프로그래밍(Dynamic Programming)

다이나믹 프로그래밍은 복잡한 문제를 더 작은 하위 문제로 나누어 해결하는 알고리즘 설계 기법입니다.<br/>
이 방식은 다음 주요 문제해결 방식과 속성을 가지고 있습니다.

### 문제해결 방식
1. **상향식(bottom-up)**
 - 작은 부분 문제부터 차례대로 해결하여 전체 문제를 해결하는 방식입니다. 이 방식은 for 또는 while을 사용하여 구현하고, 스택 오버플로의 위험이 없습니다.<br/>
2. **하향식(top-down) - 메모이제이션**
 - 큰 문제를 작은 부분 문제로 나누어 해결하는 방식입니다. 이를 위해 ***재귀 호출***을 사용합니다. 재귀 호출을 통해 계산된 값은 따로 저장하여 중복계산을 방지할 수 있습니다.
 - 주의할점: 재귀 함수가 너무 많은 호출을 발생시켜 함수 호출에 사용되는 스택 메모리가 한계에 도달하면 스택 오버플로(Stack Overflow) 에러가 발생할 수 있습니다.

### 속성
1. **최적 부분 구조(Optimal Substructure)**: 큰 문제의 최적 해결 방법이 그것을 구성하는 작은 문제들의 최적 해결 방법으로 구성될 수 있다는 성질입니다.
2. **중복되는 하위 문제(Overlapping SubProblems)**: 동일한 작은 문제가 여러 번 반복하여 발생하는 경우, 이 문제들의 해결 결과를 저장하여 중복 계산을 방지할 수 있습니다.


### 피보나치 수열 계산 예제

이 문제는 동적 프로그래밍(DPTopDown)을 사용하여 해결할 수 있습니다.
각 위치를 상태로 하여 이전 위치에서 현재 위치까지의 최소 비용을 계산합니다.
이를 위해 가능한 모든 경로를 고려하고, 각 단계에서 최소 비용을 갱신하는 방식으로 접근합니다.

### 코드 구현
***1. bottom-up 방식 풀이***
<CodeBlock className="language-java">
    {BottomUp.content}
</CodeBlock>

***2. top-down 방식 풀이***
<CodeBlock className="language-java">
    {TopDown.content}
</CodeBlock>


[DP bottom-up 방식 코드 보기](../code/DPBottomUp.java) <br/>
[DP top-down 방식 코드 보기](../code/DPTopDown.java)